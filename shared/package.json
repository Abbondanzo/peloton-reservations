{
  "name": "shared",
  "private": true,
  "description": "Shared code between frontend and backend",
  "main": "build/index.js",
  "types": "build/index.d.ts",
  "scripts": {
    "build": "tsc"
  }
}
