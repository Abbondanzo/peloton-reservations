{
  "name": "backend",
  "private": true,
  "version": "0.0.26",
  "description": "Backend comparison service for sending notifications",
  "scripts": {
    "build": "pnpm --filter=shared build && tsc",
    "dev": "ts-node src/index.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -p tsconfig.test.json"
  },
  "dependencies": {
    "@sentry/node": "^10.48.0",
    "dotenv": "^17.4.2",
    "firebase-admin": "13.5.0",
    "shared": "workspace:*"
  },
  "devDependencies": {
    "firebase": "12.2.1",
    "ts-node": "10.9.2",
    "typescript": "5.9.2",
    "vitest": "^3.2.7"
  }
}
