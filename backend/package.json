{
  "name": "backend",
  "private": true,
  "version": "0.0.0",
  "description": "Backend comparison service for sending notifications",
  "scripts": {
    "build": "tsc",
    "start": "ts-node src/index.ts"
  },
  "devDependencies": {
    "ts-node": "10.9.1"
  }
}
